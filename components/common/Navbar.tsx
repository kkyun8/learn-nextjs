const Navbar = (): JSX.Element  => {
  return (
    <nav>
      <div className="menu">
        <a className="active">Menu</a>
        <a>Menu</a>
        <a>Menu</a>
        <a>Menu</a>
      </div>
    </nav>
  )
}

export default Navbar;